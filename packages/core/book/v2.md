# Table of Contents

1.  [Update the transports & frame fault story](#orgbeb262f)
2.  [Introduction](#org13df834)
    1.  [TLDR](#org2c7df65)
3.  [The Kernel](#org5453f0f)
    1.  [Communication](#org884e887)
    2.  [Messages](#orga6fd89f)
4.  [Transports](#org66a9dbc)
    1.  [Abstractly](#org319a7e0)
    2.  [Concretely](#org63664fc)
5.  [Deployment (Draft)](#org02083ce)
    1.  [Abstractly](#org5f3fe72)
    2.  [Concretely](#org9979f43)
6.  [Utilities](#org6527e30)
    1.  [Type Predicates](#org53a1d66)
    2.  [Misc](#orgc50ce6b)

<a id="orgbeb262f"></a>

# TODO Update the transports & frame fault story

I need to cleanup the fault handling story there.

<a id="org13df834"></a>

# Introduction

This literate document contains the source code for the core of [Bassline](https://bassline.dev) v2.

Bassline is designed to be hacked on and modified, so we encourage people to read this!

<a id="org2c7df65"></a>

## TLDR

Bassline is a system for partial information programming.

Programs in Bassline work with incomplete messages. They build local understanding from what they already know while acting on new information.

Anything outside your local perspective is encountered through communication. When that communication yields information that matters, we observe it as a resource. We do not need a complete account of what it is, where it is, or how it produces what we receive.

Identity, location, provenance, timing, and order are therefore not taken as settled facts. They are local judgments made from partial information. Different participants may have different grounds for believing them, and that is good.

The model never assumes a closed world, so growth uses the same mechanism as ordinary operation. New message shapes, participants, and behavior do not require a separate extensibility system, they arrive as more messages to interpret.

<a id="org5453f0f"></a>

# The Kernel

The kernel starts with messages and a local capability for communication. In this book that capability is implemented with ports, a minimal set of capabilities for communication. Keep in mind the important thing is the ability to exchange and observe information, not any particular interface shape.

For each part of the kernel, we will separate the abstract idea from the concrete implementation used in this book. The abstract layer explains what shape of thing we are modeling. The concrete layer shows one minimal JavaScript realization of that shape.

<a id="org884e887"></a>

## Communication

### Abstractly

Communication is the local capability to receive new information and contribute new information. Bassline treats communication as an ongoing process in which participants coordinate by processing messages and inferring enough structure from what they know to continue.

Communication needs a shape that preserves decoupling. Producers should be able to contribute information without needing a fixed understanding of any consumer, and consumers should be able to build their own understanding from the flow they observe.

### Concretely

In this book, communication is implemented as ports.

A port gives us 3 capabilities:

1.  `send(aMessage) -> void`

    Which let&rsquo;s us send a message to something

2.  `recv() -> Promise<aMessage>`

    Which let&rsquo;s us pull a message from a local buffer, blocking until one is available.

3.  `close() -> void`

    To ignore any future messages, stopping communication from our end.

A port is a minimal surface that faithfully implements what we care about from communication.

An important thing to note is that there is no builtin lifecycle propagation with ports. This is because closing a port is a local decision made when a participant decides they aren&rsquo;t interested anymore, or that the conversation is not useful to them anymore.

Senders are not notified and their messages are dropped should they send to the closed port. This ensures neither side requires knowledge of the other&rsquo;s state / status without the other side explicitly sharing this.

1.  Port implementation

    The port implementation is rather simple.

    It maintains a buffer for messages, and a queue of promises to resolve. We can give the buffer a fixed capacity, in which it will act as a sliding buffer, dropping the oldest messages. Or if we give it a capacity of 0, it will not buffer any messages, requiring a listener actively be waiting for a message, dropping all others.

    The only other thing to note is the local convenience of EOF.

    EOF is a symbol sent through and used by local port consumers to indicate that the port closed. It is not universal and will serialize over the wire meaningfully. It only exists to make our lives simpler in js. And it is trivial to build your own interpretation of what should signal EOF locally.

    You cannot send EOF directly through a port, nor should you ever have to. It should only be created internally inside of a port to notify a local consumer we are closed.

        export const EOF = Symbol.for('$$BASSLINE_EOF$$')
        export const isEOF = v => v === EOF
        export function port(size = Infinity) {
          const buffer = []
          const waiters = []
          let closed = false
          const close = () => {
            closed = true
            for (const w of waiters) w(EOF)
            waiters.length = 0
          }
          const send = msg => {
            if (isEOF(msg)) throw new Error('Bassline EOF is reserved')
            if (closed) return
            if (waiters.length > 0) return waiters.shift()(msg)
            if (buffer.length >= size) buffer.shift() // sliding buffer
            if (size > 0) buffer.push(msg) // no buffer
          }
          const recv = () => {
            if (buffer.length > 0) return Promise.resolve(buffer.shift())
            if (closed) return Promise.resolve(EOF)
            return new Promise(resolve => waiters.push(resolve))
          }
          return { send, recv, close }
        }

2.  Using a port

    The most common way to use a port, is inside of an endless async loop that exists when we recv EOF.

    We define a helper for this called `consume`. It let&rsquo;s us build actor-like processes from our ports, and is convenient.

    If you need to define more sophisticated control flow inside of the loop, just write the extra 3 lines. :)

        export async function consume(recv, callback) {
          let msg
          while (true) {
            msg = await recv()
            if (isEOF(msg)) break
            await callback(msg)
          }
        }

3.  Nets

    Current our ports look like &ldquo;point-to-point&rdquo; communication, however in Bassline we don&rsquo;t talk to &ldquo;something&rdquo;, we observe information from our perspective, and send through opaque handles.

    This let&rsquo;s us do some neat things when it comes to communication.

    For example, a common structure we use is a `net`.

    A `net` is a local structure that gives us ports backed by any number of things. Meaning that we build a compound communication interface without the consumers or producers ever knowing directly. They might be able to tell if they see more information, but they can never know for sure.

    This highlights that in bassline cooperating participants appear as a single participant, even if they are totally unaware of each other.

        export function net() {
          const ports = new Set()

          function join(size) {
            const p = port(size)
            let closed = false
            ports.add(p)
            const send = msg => {
              if (closed) return
              ports.forEach(port => port !== p && port.send(msg))
            }
            const close = () => {
              closed = true
              ports.delete(p)
              p.close()
            }

            return {
              recv: p.recv,
              send,
              close,
            }
          }
          join.close = () => [...ports].forEach(p => p.close())
          join.send = msg => [...ports].forEach(p => p.send(msg))
          return join
        }

4.  Communication without input

    Not all communication take input.

    A clock is an example of a one-sided communication participant, it can say things, but will never listen.

    It can be used with anything that wants a capability to receive information, and can be used for local chron jobs, scheduling, and similar things.

        export const clock = (ms = 1000) => {
          const p = port(1)
          const tick = () => p.send({ ts: Date.now() })
          const interval = setInterval(tick, ms)
          tick()
          return {
            recv: p.recv,
            close: () => {
              clearInterval(interval)
              p.close()
            },
          }
        }

<a id="orga6fd89f"></a>

## Messages

### Abstractly

A message is a fragment of partial information. It is whatever can be locally held, passed around, inspected, and combined without requiring a complete picture of the world.

A message does not come with privileged meaning beyond what participants give it. If provenance, correlation, naming, coordination hints, or other structure matters, it lives in the message like any other data. Bassline does not want a separate metadata plane that is somehow more real than the information being exchanged.

**Note: The following is a bit of new prose for messages i&rsquo;m testing**

Messages are data, not &ldquo;code&rdquo;. They are inert unless interpreted and acted upon. Interpreting a message is the local act of &ldquo;binding&rdquo; the meaning of message to some local program execution.

As such it doesn&rsquo;t violate our model of messaging & communication, if there exists purely local capabilities bound by other local participants, traveling ALONGSIDE the data.

This is equivalent to ports, we don&rsquo;t care who added these capabilities, but we can observe them and act on them locally.

This isn&rsquo;t possible in all systems, but it&rsquo;s a useful pattern none the less.

The constraints to make this &ldquo;work&rdquo; are:

- capabilities must be allowed to be ignored
- capabilities must never return values directly, they should be opaque
- capabilities shouldn&rsquo;t removed data from the message itself, they should only enrich it for those interested
- capabilities cannot travel over the wire, unless they are reified as a higher order message.

### Concretely

A message is a plain object with string keys. It can be empty, partially filled, or nested inside other messages.

The concrete representation is intentionally minimal. Plain objects are open-ended, nest naturally, and are easy to serialize, enrich, and inspect. That keeps the message lightweight and avoids forcing a closed protocol up front.

### Simple Messages

`message()` normalizes its input into a message:

- plain objects pass through
- `undefined` becomes `{}`
- Anything else is wrapped as `{body: value}`.

      import { isPlainObject, isString, isSymbol, isFunction, hasKeys } from './utils.js'

      export const isEmpty = msg => Object.keys(msg).length === 0
      export const message = content => {
        if (content === undefined) return {}
        if (isPlainObject(content)) return { ...content }
        return { body: content }
      }

`update`, as the name implies, updates a message using a function.

Calling update with two arguments `update(msg, fn)` will apply `fn(msg)`, and normalize the output as a message. Calling update with a single argument is curried, `update(fn)` produces a function `msg -> update(msg, fn)`.

    export function update(...args) {
      switch (args.length) {
        case 1: {
          const [fn] = args
          return msg => message(fn(msg))
        }
        case 2: {
          const [msg, fn] = args
          return message(fn(msg))
        }
        default:
          throw new Error(`invalid update arity: ${args.length}`)
      }
    }

### Faults

This indicates that local processing cannot continue, and we must stop.

You don&rsquo;t have to use this construct, but it&rsquo;s convenient since it&rsquo;s message aware.

    export class Fault extends Error {
      constructor(condition, msg, context = {}) {
        super(`fault: ${condition}`)
        this.condition = condition
        this.msg = msg
        this.context = context
      }
      toMessage() {
        return message({ condition: this.condition, msg: this.msg, ...this.context })
      }
    }

    export const fault = (condition, msg, context) => new Fault(condition, msg, context)

### Higher Order Messages

As you are building with Bassline you may find yourself in a situation where you want to add data to a message, but that data isn&rsquo;t relevant to the message, but it&rsquo;s related to it.

This is a very strong smell of a leaky ontology, and you should consider what you are trying to express. Are you trying to say something about the message? Compare it to another message? All of these would call for &ldquo;higher order messages&rdquo;.

A higher order message is simply a message where the data is other messages. _Isn&rsquo;t this just a nested object?!_ Structurally sure, but not semantically.

A message is a unit of information about something. If I am expressing 2d coordinates a fragment like `{x: 123}` isn&rsquo;t enough. What I am trying to express is a 2d coordinate, so `{x: 123, y: 456}` is enough for me. Anything smaller and i&rsquo;m lacking essential information for someone to derive meaning.

So &ldquo;message-ness&rdquo; isn&rsquo;t a black and white property, it&rsquo;s a matter of what we are trying to express, and whether the message is complete with regards to that.

**Note: None of these are mandates! These are just useful patterns we have discovered while using Bassline!**

**Bonus Note: Higher order messages let us lift any message interpretation as a sharable message**

This lets us do things like partially apply a message, build things like schedulers, compilers, and routers using nothing more than messages. In fact let in is effectively a dumb compiler, and composing them gives us something like a nano-pass compiler.

1.  Let in

    The higher order message, `let:in:` can be used as a binding message. Allowing us to express a single pass rewrite via a message.

    `subst` implements this binding convention.

    It treats `let` as a local binding scope and `in` as the deferred message body. Expansion is intentionally tiny:

    - it only expands when both `let` and `in` are plain objects
    - it rewrites only the `in` branch
    - it replaces string leaves that match names in `let`
    - replacement is a single pass

    `lift` does the opposite, given a message, it will extract all keys from a message, producing a `let:in:` that reduces to the message.

        export function subst(msg) {
          switch (true) {
            case !hasKeys(msg, ['let', 'in']):
              throw fault('expand requires msg to have keys let & in')
            case !isPlainObject(msg.let):
            case !isPlainObject(msg.in):
              throw fault('msg.in & msg.let must be objects')
            default:
              return rewrite(msg.let, msg.in)
          }
        }

        export function lift(msg) {
          const let_ = {}
          const in_ = { ...msg }
          for (const f of Object.keys(msg)) {
              let_[f] = msg[f]
              in_[f] = f
          }
          return { let: let_, in: in_ }
        }

        function rewrite(bindings, value) {
          switch (true) {
            case isString(value) && value in bindings:
              return bindings[value]
            case Array.isArray(value):
              return value.map(item => rewrite(bindings, item))
            case isPlainObject(value): {
              const out = {}
              for (const [k, v] of Object.entries(value)) out[k] = rewrite(bindings, v)
              return out
            }
            default:
              return value
          }
        }

### Messages with capabilities

This is a reference implementation of a message with capabilities bound by another participant.

The interesting things to note here are the this allows us to express how a local participant can act on a message, allowing us share a send with a message.

This makes messages not just data, but data enhanced with associated behavior through an interaction surface.

By closing over a function that hides our acceptance criteria, we bind a capability to behaviorally test a receiver without ever assuming that the outside world exists or is stable.

For example, we can build TCP like acknowledgement flows. But what&rsquo;s interesting is that the receiver of the message, cannot know what the final destination / intent of the response will be.

This asymmetry falls naturally out of the model we have, and is an extremely powerful tool for opaquely testing and validating how peers are behaving.

Consider traditional &ldquo;challenge&rdquo; protocols. The person being challenged knows that they are being challenged as they are engaging in a shared common protocol.

Whereas if messages carry capabilities, the recipient of a message only sees the capability along with the data, and they never know if the message is a &ldquo;test&rdquo;, as that&rsquo;s a private perspective only the capability provider knows. It also lets them add / remove / replace capabilities as they process and share the messages.

    export const hasCap = (msg, name) => msg[name] && isFunction(msg[name])

    function assertHandlers(handlers) {
      const syms = Object.getOwnPropertySymbols(handlers)
      for (const sym of syms) {
        if (!isSymbol(sym)) throw new Error('invalid handler key, must be a symbol!')
        if (!isFunction(handlers[sym])) throw new Error('invalid handler,  must be a function')
      }
      return syms
    }

    export function offer(dest, handlers) {
      const syms = assertHandlers(handlers)
      return msg => {
        const enriched = { ...msg }
        for (const sym of syms) {
          enriched[sym] = m => void handlers[sym](m)
        }
        dest(enriched)
      }
    }

    export function accept(handlers) {
      const syms = assertHandlers(handlers)
      if (syms.length === 0) throw new Error('[accept] invalid handlers object, no symbols')
      return async msg => {
        for (const sym of syms) {
          if (hasCap(msg, sym)) await handlers[sym](msg[sym], msg)
        }
      }
    }

<a id="org66a9dbc"></a>

# Transports

As above, it helps to separate the abstract role of a transport from the concrete transport used in this book.

<a id="org319a7e0"></a>

## Abstractly

A transport adapts some external medium into local communication capabilities. This matters because the programming model should not change just because information crossed a process boundary, a machine boundary, or a device boundary. Local and non-local communication differ in mechanics, but they should not require different conceptual tools.

<a id="org63664fc"></a>

## Concretely

Every transport produces the same thing: a port with `send`, `recv`, and `close`. What differs is the medium underneath and whether that medium carries raw bytes or discrete messages.

Byte transports (TCP sockets, stdio, serial) carry raw chunks. They need a framing layer to delimit and parse messages. Message transports (WebSocket, WebWorker/MessagePort) carry discrete messages natively and skip framing entirely.

### Framing

Framing is separated from any specific transport. `readFrame` bridges a byte-level `recv` to a message-level `send`: it consumes chunks, accumulates them in a buffer, splits on newlines, and emits parsed messages. `format` is a plain function that serializes a message as a JSON line.

    import { message } from '../messages.js'
    import { consume } from '../comm.js'

    export function readFrame(recv, send) {
      let buffer = ''
      consume(recv, chunk => {
        buffer += chunk
        let nl
        while ((nl = buffer.indexOf('\n')) !== -1) {
          const line = buffer.slice(0, nl)
          buffer = buffer.slice(nl + 1)
          if (!line) continue
          try {
            send(message(JSON.parse(line)))
          } catch (e) {
            console.error('readFrame parse error: ', e)
          }
        }
      })
    }

    export const format = msg => JSON.stringify(msg) + '\n'

    export default { read: readFrame, format }

`readFrame` fires off a `consume` loop in the background. One input chunk may produce zero or many messages. `format` is used on the outgoing side to serialize before writing.

### Socket

A socket is a byte transport. `fromSocket` creates a raw port for byte chunks, a message port for parsed messages, and bridges them through `frame.read`. Outgoing messages are formatted and written directly to the socket.

    import net from 'node:net'
    import { port } from '../comm.js'
    import defaultFrame from '../frame/jsonl.js'

    export function fromSocket(socket, frame = defaultFrame) {
      const raw = port()
      const msgs = port()

      socket.on('data', chunk => raw.send(chunk.toString()))
      socket.on('close', () => raw.close())
      socket.on('error', () => raw.close())

      frame.read(raw.recv, msgs.send)

      return {
        recv: msgs.recv,
        send: msg => socket.write(frame.format(msg)),
        close: () => {
          msgs.close()
          raw.close()
          socket.destroy()
        },
      }
    }

    export function connect(options = {}, frame = defaultFrame) {
      return fromSocket(net.createConnection(options), frame)
    }

### Serving (TCP)

A TCP server listens on a socket and produces a port of connections. Each connection is itself a port, produced by `fromSocket`.

    import nodeNet from 'node:net'
    import { fromSocket } from '../transports/socket.js'
    import { port } from '../comm.js'

    export function serve(options = {}, frame) {
      const p = port()
      const server = nodeNet.createServer(socket => {
        p.send(fromSocket(socket, frame))
      })
      server.listen(options)
      server.on('close', () => p.close())
      server.on('error', () => p.close())
      return {
        recv: p.recv,
        close: () => {
          p.close()
          server.close()
        },
        server,
      }
    }

### Serving (WebSocket)

A WebSocket server wraps an existing `WebSocketServer` instance and produces a port of connections, matching the TCP serve API shape. The caller brings their own `WebSocketServer` — no `ws` dependency in core.

    import { fromWebSocket } from '../transports/websocket.js'
    import { port } from '../comm.js'

    export function serve(wss) {
      const p = port()
      wss.on('connection', ws => p.send(fromWebSocket(ws)))
      wss.on('close', () => p.close())
      wss.on('error', () => p.close())
      return {
        recv: p.recv,
        close: () => {
          p.close()
          wss.close()
        },
        wss,
      }
    }

### WebSocket

A WebSocket is a message transport. Each `ws.send()` is one discrete message, so no framing is needed. This works in the browser with native `WebSocket`, in Node.js with the `ws` package, and for `RTCDataChannel` since it has the same event interface.

    import { port } from '../comm.js'
    import { message } from '../messages.js'

    export function fromWebSocket(ws) {
      const p = port()
      ws.addEventListener('message', e => {
        try {
          p.send(message(JSON.parse(e.data)))
        } catch (e) {
          console.error('failed to parse: ', e)
        }
      })
      ws.addEventListener('close', () => p.close())
      ws.addEventListener('error', () => p.close())

      return {
        recv: p.recv,
        send: msg => ws.send(JSON.stringify(msg)),
        close: () => {
          p.close()
          ws.close()
        },
      }
    }

### WebWorker

A `MessagePort` is a message transport that uses structured clone. Objects cross the boundary without serialization. Works with `Worker`, `MessagePort`, `SharedWorker.port`, and `BroadcastChannel`.

    import { port } from '../comm.js'
    import { message } from '../messages.js'

    export function fromPort(messagePort) {
      const p = port()
      messagePort.onmessage = e => p.send(message(e.data))
      messagePort.onmessageerror = () => p.close()

      return {
        recv: p.recv,
        send: msg => messagePort.postMessage(msg),
        close: () => p.close(),
      }
    }

### Stdio

Stdio is a byte transport, like TCP, and uses framing internally to produce a port of messages.

    import readline from 'node:readline'
    import { port } from '../comm.js'
    import defaultFrame from '../frame/jsonl.js'

    export function fromStdio(frame = defaultFrame) {
      const raw = port()
      const msgs = port()
      const rl = readline.createInterface({ input: process.stdin })

      rl.on('line', line => raw.send(line + '\n'))
      rl.on('close', () => raw.close())

      frame.read(raw.recv, msgs.send)

      return {
        recv: msgs.recv,
        send: msg => process.stdout.write(frame.format(msg)),
        close: () => {
          msgs.close()
          raw.close()
          rl.close()
        },
      }
    }

<a id="org02083ce"></a>

# Deployment (Draft)

This is a stub, so you can ignore this for now!

Deployment also has an abstract layer and a concrete one.

<a id="org5f3fe72"></a>

## Abstractly

A bassline system is deployed by making communication available somewhere and wiring local logic to it. Deployment is not where the system acquires a fixed command surface or a closed set of capabilities. It is where some part of the world becomes reachable for communication.

The reason this stays lightweight is the same reason the rest of the model stays lightweight: coordination is driven by message flow and local interpretation, not by a privileged deployment schema.

<a id="org9979f43"></a>

## Concretely

A bassline system can run as a daemon, a long-lived process listening on a socket. Interacting with it means obtaining a communication capability and exchanging messages.

### The daemon is just a server with a recv loop

A boot script opens a socket and starts consuming connections. Each connection is a port. What the daemon does with those messages is determined by whatever logic is wired in. There is no fixed set of capabilities.

    import { serve } from './serve/tcp.js'
    import { consume } from './comm.js'

    const { recv } = serve({ path: '~/.bassline/sock' })

    consume(recv, async conn => {
      consume(conn.recv, msg => conn.send(handle(msg)))
    })

The `handle` function is where all the logic lives. It could route messages onward, evaluate expressions, update state, or do nothing. The daemon just opens the door.

### A REPL is just a transport

A command-line interface is a connection over stdio instead of a socket. `fromStdio()` gives you a port backed by stdin/stdout with the same framing as TCP.

    import { fromStdio } from './transports/stdio.js'

    const { send, recv } = fromStdio()

The downstream logic sees messages either way, regardless of whether the transport is a socket, a WebSocket, a terminal, or whatever else.

### Convention

By convention, a bassline daemon keeps its state under `~/.bassline/`, with `BASSLINE_HOME` as an override. The socket lives there. What else lives there (persistence files, logs, configuration) depends on what the boot script sets up.

The boot script is the only thing that&rsquo;s &ldquo;configured&rdquo; in the traditional sense. Everything else emerges from the messages the system receives after it starts listening.

<a id="org6527e30"></a>

# Utilities

<a id="org53a1d66"></a>

## Type Predicates

<table border="2" cellspacing="0" cellpadding="6" rules="groups" frame="hsides">

<colgroup>
<col  class="org-left" />

<col  class="org-left" />
</colgroup>
<thead>
<tr>
<th scope="col" class="org-left">Predicate</th>
<th scope="col" class="org-left">Tests for</th>
</tr>
</thead>
<tbody>
<tr>
<td class="org-left"><code>isArray</code></td>
<td class="org-left"><code>Array.isArray</code></td>
</tr>

<tr>
<td class="org-left"><code>isNil</code></td>
<td class="org-left"><code>null</code> or <code>undefined</code></td>
</tr>

<tr>
<td class="org-left"><code>isPromise</code></td>
<td class="org-left"><code>instanceof Promise</code></td>
</tr>

<tr>
<td class="org-left"><code>isNumber</code></td>
<td class="org-left"><code>typeof ==</code> &rsquo;number&rsquo;=</td>
</tr>

<tr>
<td class="org-left"><code>isString</code></td>
<td class="org-left"><code>typeof ==</code> &rsquo;string&rsquo;=</td>
</tr>

<tr>
<td class="org-left"><code>isFunction</code></td>
<td class="org-left"><code>typeof ==</code> &rsquo;function&rsquo;=</td>
</tr>

<tr>
<td class="org-left"><code>isPlainObject</code></td>
<td class="org-left"><code>prototype =</code> Object.prototype=</td>
</tr>

<tr>
<td class="org-left"><code>hasKeys</code></td>
<td class="org-left">Check whether an object has all of the specified keys</td>
</tr>
</tbody>
</table>

    export const isArray = v => Array.isArray(v)
    export const isNil = obj => obj == null
    export const isPromise = obj => obj instanceof Promise
    export const isNumber = v => typeof v === 'number'
    export const isString = v => typeof v === 'string'
    export const isFunction = v => typeof v === 'function'
    export const isSymbol = v => typeof v === 'symbol'
    export const isNull = v => v === null
    export const isPlainObject = obj => {
      if (obj == null) return false
      return Object.getPrototypeOf(obj) === Object.prototype
    }
    export const hasKeys = (obj, keys = []) => {
      if (isNil(obj)) return false
      const requiredKeys = isArray(keys) ? keys : [keys],
        objectKeys = new Set(Object.keys(obj))

      return requiredKeys.every(key => objectKeys.has(key))
    }
    export const not = predicate => value => !predicate(value)
    export const constant = value => () => value
    export const identity = value => value
    export const delay = async (ms = 1000) => {
        await new Promise(res => setTimeout(res, ms))
        return
    }

<a id="orgc50ce6b"></a>

## Misc

    export const castArr = value => (isArray(value) ? value : [value])

<footer class="book-footer">
  <div class="footer-links">
    <a href="https://bassline.dev">bassline.dev</a>
    <span class="separator">&middot;</span>
    <a href="https://github.com/Bassline-Org/bassline">GitHub</a>
  </div>
  <p class="footer-license">AGPLv3</p>
</footer>

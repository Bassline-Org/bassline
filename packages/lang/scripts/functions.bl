reducedContext: project system [ + make fn! reduce ]

fn: make fn! self [args body] [
    compose [make fn! self (args) (body)]
]

double: fn [x] [ * x 2]
print :double

hof: make fn! reducedContext [a] [
    make fn! self [b] [
        + a b
    ]
]

foo: hof 10
bar: hof 20

print map (iota 10) :foo
commands: map
    (iota 10)
    (make fn! self [x] [ compose [ print (x)] ])
print commands

reduce commands
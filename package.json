{
  "name": "bassline",
  "version": "0.1.0",
  "private": true,
  "description": "Getchyo groove on",
  "repository": {
    "type": "git",
    "url": "https://github.com/MercuricChloride/bassline.git"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0",
  "scripts": {
    "start": "pnpm --filter @bassline/web start",
    "dev": "pnpm --filter @bassline/web dev",
    "dev:wc": "pnpm --filter @bassline/web-components dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "lodash": "^4.17.21"
  }
}
/**
 * Atto-Bassline Propagation UI Demo
 * Shows React components that ARE gadgets participating in the propagation network
 */

import { DemoApp } from '../../../../atto-bassline/src/demo-app'

export default function AttoDemo() {
  return <DemoApp />
}
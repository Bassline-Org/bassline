// Configuration constants for the editor
// Exported from a separate file to avoid circular dependencies

// Remote mount prefix - access daemon resources via bl:///r/*
export const REMOTE_PREFIX = 'bl:///r'
